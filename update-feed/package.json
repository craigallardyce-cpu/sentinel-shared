{
  "name": "@sentinel/update-feed",
  "version": "0.1.0",
  "private": true,
  "description": "The desktop update feed's address and the About panel's version check, for the fleet's backends. Reads the public feed the website serves, because the app repositories and their GitHub Releases are private.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist-cjs/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.cjs.json && node scripts/mark-cjs.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vitest": "^3.2.4",
    "@types/node": "^22.10.2"
  }
}
